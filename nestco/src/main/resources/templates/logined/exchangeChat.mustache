<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/socket.js"></script>
    <title>교환 채팅</title>
</head>
{{>layouts/header}}
<body>
<div class="container mt-5">
    <!-- 채팅창 -->
    <div id="chatWindow">
        <h2>채팅창</h2>
        <div id="messages"></div>
        <input id="messageInput" type="text" placeholder="메시지 입력" class="form-control mt-3">
        <button class="btn btn-primary mt-2" onclick="sendMessage()">메시지 보내기</button>
    </div>

    <!-- 거래 상태 선택 폼 -->
    <form id="transactionStatusForm" style="display: none;" class="mt-5">
        <h2>거래 상태 선택</h2>
        <button class="btn btn-success" onclick="updateTransactionStatus({{transactionId}}, true)">거래 완료</button>
        <button class="btn btn-danger" onclick="updateTransactionStatus({{transactionId}}, false)">거래 미완료</button>
    </form>

    <!-- 거래 완료 또는 미완료 상태 표시 메시지 -->
    <h2>{{message}}</h2>
    <a href="/home" class="btn btn-secondary">홈으로</a>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
{{>layouts/footer}}
</html>
